services:
  api:
    container_name: api
    image: soilapt-api:latest
    volumes:
      - files:/usr/src/app/files
    ports:
      - 3001:3001
    env_file:
      - path: .env

  bot:
    container_name: bot
    depends_on:
      - api
    image: soilapt-bot:latest
    env_file:
      - path: .env
